#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int a=10, b, c, d, e, f;  // comentario, 1
string x = "hola";
float w;
double y = 1;  // = comentario 2
y = 2;

// comentario de línea
int miFunc();

/* comentario 3 c1 de 
   multiples líneas */
int miFunc2 (int par1) {   
    int c1 = 15;
    if (a < 10) {
        a = a + 1;
    }

    int temp = 0;
    while (temp < 10) {
        temp = temp + 1;
    }
}
